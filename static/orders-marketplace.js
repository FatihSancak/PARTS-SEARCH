'use strict';
(() => {
  const language=['tr','de','en'].includes(localStorage.getItem('lang'))?localStorage.getItem('lang'):'tr';
  const closeLabel={tr:'Kapat',de:'Schließen',en:'Close'}[language];
  new MutationObserver(records=>records.forEach(record=>record.addedNodes.forEach(node=>{if(node.nodeType!==1)return;const button=node.matches?.('.order-gallery .og-head button')?node:node.querySelector?.('.order-gallery .og-head button');if(button){button.textContent='×';button.title=closeLabel;button.setAttribute('aria-label',closeLabel)}}))).observe(document.documentElement,{childList:true,subtree:true});
  const logos={ebay:'ebay.svg',ovoko:'ovoko.svg',teilehaber:'teilehaber.svg',autoteilemarkt:'autoteilemarkt.svg',partsbits:'partsbit.svg',partsbit:'partsbit.svg',opisto:'opisto.svg'};
  const key=value=>Object.keys(logos).find(name=>String(value||'').toLowerCase().replace(/[\s-]/g,'').includes(name));
  const decorate=orders=>document.querySelectorAll('.order').forEach(card=>{const code=[...card.querySelectorAll('.meta span')].find(item=>item.textContent.includes(':'))?.textContent.split(':').slice(1).join(':').trim();const order=orders.find(item=>item.orderCode===code);if(!order||card.dataset.enriched)return;card.dataset.enriched='true';const meta=card.querySelector('.meta');const market=key(order.source);if(market){const badge=document.createElement('span');badge.className='marketplace-badge';const image=new Image();image.src=`/marketplace-logos/${logos[market]}`;image.alt=order.source;badge.append(image);if(market==='teilehaber')badge.classList.add('marketplace-teilehaber');meta.querySelector('.source')?.replaceWith(badge)}if(order.productUrl){const link=document.createElement('a');link.href=order.productUrl;link.target='_blank';link.rel='noopener';link.textContent='Recycle ↗';link.style.cssText='padding:4px 8px;border:1px solid #168052;border-radius:6px;color:#168052;font-size:11px;font-weight:800;text-decoration:none';meta.append(link)}});
  const load=async()=>{const from=document.querySelector('#from'),to=document.querySelector('#to');if(!from?.value||!to?.value)return;const parse=value=>{const [year,month,day]=value.split('-').map(Number);return{year,month,day}};try{const response=await fetch('/api/recycle/orders',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({from:parse(from.value),to:parse(to.value)})});const data=await response.json();if(!response.ok)return;const observer=new MutationObserver(()=>decorate(data.orders||[]));observer.observe(document.querySelector('#orders'),{childList:true,subtree:true});decorate(data.orders||[])}catch{}};
  if(document.readyState==='complete')setTimeout(load,250);else window.addEventListener('load',()=>setTimeout(load,250));
})();
